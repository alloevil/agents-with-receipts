# 贡献指南

## 收录标准

**incidents/**（核心板块，门槛最高）：

1. 必须有证据链：公开 commit / issue / 可复现步骤，读者能自己点开验证
2. 必须有「沉淀」一节：事故变成了哪条规则 / 哪个测试 / 哪个 lint。没有沉淀的事故只是故事
3. 必须有可迁移的通用教训，一句话说得清
4. 匿名化：涉及私有仓库时提供脱敏后的最小复现，不接受"我司内部出过事"式无证据叙述

**practices/**：每条断言必须有出处（官方文档/官方博客优先），标注验证日期。

**rosetta/**：每个单元格注明官方文档核实还是二手来源（`†` 标注）；修正过期信息的 PR 请附官方链接。

**tools/**：零依赖优先；必须带测试；Node ≥ 20。

## 内容纪律

- 中文为主，术语保留英文原文（AGENTS.md、hooks、sandbox…）
- 半衰期意识：发现过期内容，**删除或修正**，不要堆"历史版本"
- 宁缺毋滥：拿不准的不收

## 提交流程

1. Fork & PR，一条事故/一个主题一个 PR
2. incidents 编号取当前最大值 +1，同时更新 `incidents/README.md` 索引表
3. 跑 `npm test` 确保工具测试全绿
