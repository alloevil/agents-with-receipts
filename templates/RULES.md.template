# 硬性规则

<!--
粘性规则模板（omp 的 .omp/RULES.md；其它工具用各自的 always-apply 机制，见 rosetta/）。
只放 3-6 条"永不失守"的硬要求——它们会在长对话中被反复重新注入，每条都持续付费。
大块背景放 AGENTS.md，不放这里。
-->

- <禁令一：如"测试只用 X runner，不引入其它测试框架">
- <禁令二：如"本仓库提交必须用仓库 local git 身份">
- <禁令三：如"任何失败路径必须以非零退出码收场">
