# <项目名> — Agent 指南

<!--
从真实项目提炼的骨架。用法：
1. 删掉所有 <尖括号占位> 并填入你的事实——填不出来的整节删掉
2. 每条都应该是 agent 无法从代码推断的；写完跑 agentsmd-lint 检查
3. 控制在 150 行内；agent 犯一次错补一条，从没用过的条目删掉
-->

<一句话：项目是什么 + 技术栈 + 一个最重要的架构事实>

## 常用命令

| 任务 | 命令 |
| --- | --- |
| 全部测试 | `<npm test / cargo test / ...>` |
| 单文件测试 | `<精确命令，带路径示例>` |
| Lint / 格式化 | `<命令>` |
| 本地运行 | `<命令>`（<端口/入口等一句话>） |
| <项目特有的高危命令> | `<命令>`（<警告：什么情况下不要跑>） |

## 环境变量

- `<VAR>` — <作用；默认值>

## 安全与隐私

- <哪些文件是凭据/私人数据：绝不进提交、日志、issue、公开产物>
- <网络暴露约束，如：服务只监听 127.0.0.1>

## 边界

- **不要动**：<生成物目录>、<第三方 vendored 代码>、<数据目录>
- **先问再做**：新增运行时依赖、改 CI 配置、force push、<项目特有的高危操作>

## 硬契约

<!-- 只放违反过、出过事故的；每条说清"为什么"，最好指向 incident/commit -->

- <契约一：判据/不变量 + 一句话事故背景>
- <契约二>

## 测试哲学

- <用什么 runner；mock 的边界在哪>
- <什么不测（如 CLI 编排、真实外部调用）及为什么>

## 构建与发布

- <"改了 X 必须重建 Y" 类的缓存陷阱>
- <版本 bump 涉及哪几个文件；发布流程一句话>
- <提交信息/分支命名约定>
